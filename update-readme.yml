name: Update README

on:
  schedule:
    - cron: "10 16 * * *" # Runs every day at 5:00 PM UTC

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Update README
        run: |
          # Your script to update the README file
          echo "This is a test update." >> README.md

      - name: Commit and Push Changes
        run: |
          git config --local user.email "issadiop2580@gmail.com"
          git config --local user.name "issa2580"
          git add .
          git commit -m "Automated README update"
          git push
