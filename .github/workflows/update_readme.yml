name: Update README all day at 04:04

on:
  push:
  workflow_dispatch:
  schedule:
    # “At 13:37 on every day-of-week from Monday through Friday.”
    - cron: '0 18 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
        steps:
          - name: Update README
            run: node index.js > README.md
          
          - name: Commit Updated README
            run: |
              git config --global user.name 'issa2580'
              git config --global user.email 'issadiop2580@gmail.com'
              git add README.md
              git commit -m "Mise à jour du README"
          
          - name: Push Changes
            uses: ad-m/github-push-action@master
            with: 
              github_token: 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

